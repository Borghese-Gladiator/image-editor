import React from 'react'
import Container from '@material-ui/core/Container'

export default function EditPage(props) {
  const { a } = props;
  return (
    <Container>
      <h2 style={{textAlign: "center"}}>Image Editor</h2>
    </Container>
  )
}